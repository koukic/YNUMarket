.keep-shopping.pv1.mt4.has-text-right
  = link_to '他の商品を見る', root_path, class: 'button is-warning'
hr
section.section
  = render(@cart.line_items)
  .columns
    .column
      = button_to 'リストを空にする', @cart, method: :delete, data: { confirm: "本当にリストを空にしますか？ " }, class: "button is-danger"
    .column.total.has-text-right
      h4.title.is-4
